export enum ENUM_POLICY_ACTION {
  MANAGE = 'manage',
  READ = 'read',
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
  EXPORT = 'export',
  IMPORT = 'import',
  WRITE = 'write',
}
export enum ENUM_POLICY_REQUEST_ACTION {
  MANAGE,
  READ,
  CREATE,
  UPDATE,
  DELETE,
  EXPORT,
  IMPORT,
  WRITE,
}
export enum ENUM_POLICY_SUBJECT {
  API_KEY = 'API_KEY',
  SETTING = 'SETTING',
  ROLE = 'ROLE',
  USER = 'USER',
  SHOP = 'SHOP',
  PRODUCT = 'PRODUCT',
}
